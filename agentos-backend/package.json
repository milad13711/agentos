{
  "name": "agentos-backend",
  "version": "0.1.0",
  "description": "AgentOS real backend — MVP/V1 slice. Pure Node.js (no external dependencies): node:http + node:sqlite + node:crypto.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "socks-proxy-agent": "^10.1.0"
  }
}
