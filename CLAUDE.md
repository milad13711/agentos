# AgentOS — پروژه Agent-First CRM فارسی

## برای session بعدی (خلاصه‌ی وضعیت)

همه‌چیز commit و push شده — برنچ فعلی `claude/continue-gk70lc` روی GitHub (`milad13711/agentos`) کاملاً به‌روزه، هیچ PR ای هنوز باز نشده (اگه لازم شد، صریح درخواست بده). ⚠️ توجه: workflow دیپلوی (`.github/workflows/deploy.yml`) هنوز فقط روی push به `claude/agentos-phase-0-1-setup-vcjcz8` تریگر می‌شه (گیر #۸ پایین) — یعنی push به این برنچ جدید *خودکار* دیپلوی نمی‌شه تا وقتی که کاربر صریحاً بخواد اون workflow آپدیت بشه یا این برنچ merge/rename بشه. قبل از هر کاری، `git log --oneline -20` و کل این فایل رو بخون تا context کامل باشه؛ سندهای تکمیلی: `docs/phase1-event-schema-agent-roles.md` (طراحی Event/dispatch) و `docs/ci-cd-setup.md`. لیست کامل کارهای انجام‌شده و باقی‌مونده دقیقاً همین‌جا پایین‌تره — چیز مهمی خارج از این فایل و خودِ کد نیست.

⏳ **آخرین کار (۲۰۲۶-۰۷-۲۶، کد نوشته و تست شده، هنوز روی سرور دیپلوی/تست‌نشده)**: قابلیت مکالمه صوتی با Agent — هم تو وب‌چت هم تو تلگرام. جزئیات کامل در گیر #۱۶ پایین‌تر. **کاربر باید `OPENAI_VOICE_API_KEY` (یک کلید واقعی و مستقیم OpenAI، جدا از `OPENAI_API_KEY` که GapGPT‌ه) رو تو `.env` سرور ست کنه تا این فیچر فعال بشه** — بدونش، دکمه میکروفون تو وب و وویس‌های تلگرام یک پیام «قابلیت صوتی فعال نشده» می‌گیرن، هیچ کرشی نمی‌شه.

## معماری کلی

```
agentos-backend/   → عمدتاً Node.js خالص — node:http + node:crypto + (SQLite پیش‌فرض با node:sqlite، یا PostgreSQL واقعی وقتی DATABASE_URL ست بشه، با تنها dependency واقعی: `pg`)
agentos-web/        → Next.js 14 App Router — BFF pattern، توکن AgentOS فقط سمت سرور، httpOnly cookie
agentos-deploy/     → docker-compose.yml + Caddyfile + schema-postgres.sql (وایر شده، پایین رو ببین) + DEPLOY.md
frontend-local/     → (داخل agentos-backend) دو HTML مستقل بدون build، برای تست سریع بدون Next.js
```

## دستورات مهم

```bash
# بک‌اند (لوکال)
cd agentos-backend && node src/server.js

# فرانت (لوکال)
cd agentos-web && npm run dev

# دیپلوی کامل (روی سرور)
docker compose up -d --build
docker compose logs -f
docker compose exec backend node -e "..."   # اجرای اسکریپت مستقیم روی دیتابیس زنده

# ریست کامل دیتابیس محلی
rm -rf agentos-backend/data
```

## وضعیت استقرار فعلی

* سرور: `94.182.93.52` (Ubuntu 22.04، Hetzner/مشابه)، SSH: `root@94.182.93.52`
* فایل‌ها روی سرور: `~/agentos-backend`, `~/agentos-web`, `~/docker-compose.yml`, `~/Caddyfile`, `~/.env`
* دامنه: `exirsms.ir` — ✅ کامل جا افتاده (Cloudflare Name Server از طریق پنل IRNIC، رکورد A به‌صورت DNS-only/ابر خاکستری تا Let's Encrypt مستقیم بتونه challenge رو حل کنه)
* ✅ **HTTPS واقعی فعاله** — Caddy با `Caddyfile` روی `exirsms.ir, www.exirsms.ir` گواهی Let's Encrypt گرفته (auto-renew)، ریدایرکت خودکار HTTP→HTTPS (308) کار می‌کنه. `ALLOW_INSECURE_COOKIE` از `docker-compose.yml` حذف شد؛ کوکی نشست الان `Secure` واقعیه.
* AI Gateway: GapGPT (پروکسی ایرانی سازگار با OpenAI API) با مدل `gapgpt-qwen-3.5`، `OPENAI_BASE_URL=https://api.gapgpt.app/v1`
* Zarinpal: در حالت Sandbox (`ZARINPAL_SANDBOX=1`)، هنوز Merchant ID واقعی نداره

## گیرهای مهم (وقت زیادی صرف کشفشون شد — دوباره بهشون گیر نکن)

1. `env-loader.js` اولین مقدار رو نگه می‌داره، نه آخری — اگه یک متغیر توی `.env` دوبار تعریف بشه، خط اول برنده‌ست. همیشه بعد از ویرایش `.env`، با `grep -c "^KEY="` چک کن که هر متغیر دقیقاً یک‌بار باشه.
2. Next.js Dockerfile نیاز به پوشه `public/` داره (`COPY --from=builder /app/public ./public`) — اگه این پوشه نباشه build fail می‌شه. یک `public/robots.txt` نمونه از قبل توی پروژه هست، حذفش نکن.
3. کوکی نشست (`lib/session.ts`) با `secure: NODE_ENV==='production'` — روی HTTP خام (بدون TLS)، این باعث می‌شه مرورگر کوکی رو اصلاً ذخیره نکنه (لاگین "بی‌صدا" fail می‌شه). ✅ دیگه موضوعیت نداره چون HTTPS واقعی فعاله؛ اگه یه سرور/محیط جدید دوباره روی HTTP خام تست می‌کنید، یادتون باشه `ALLOW_INSECURE_COOKIE=1` موقتاً لازمه.
4. بک‌اند در `docker-compose.yml` به بیرون expose نمی‌شه (عمداً — کلید AI هیچ‌وقت نباید در معرض شبکه باشه). فقط از طریق شبکه داخلی Docker با `web` صحبت می‌کنه. اگه خواستی مستقیم تستش کنی: `docker compose exec web wget -qO- http://backend:8787/api/health`
5. ✅ **رفع شد (فاز ۱)**: منطق `create_contact`/`create_deal` و بقیه اکشن‌های نویسا دیگه دوبار پیاده‌سازی نشده — یک `dispatch()` واحد در `agentos-backend/src/actions.js` که هم `server.js` هم `agent.js` صداش می‌زنن. جزئیات کامل در `docs/phase1-event-schema-agent-roles.md`.
6. Node 22 لازمه (نه کمتر) — چون از `node:sqlite` استفاده می‌کنیم که Native و بدون هیچ dependency خارجیه.
7. این پروژه یک Git repository واقعی داره، روی GitHub (`milad13711/agentos`). هر تغییری باید commit و push بشه — دیگه کپی‌کردن zip/scp دستی نکن. (برنچ کاری قبلاً `claude/agentos-phase-0-1-setup-vcjcz8` بود، الان `claude/continue-gk70lc` — به گیر #۸ توجه کن، workflow دیپلوی هنوز روی برنچ قدیمی تریگر می‌شه.)
8. ✅ **دیگه دستی نیست** — `.github/workflows/deploy.yml` هر `git push` به برنچ `claude/agentos-phase-0-1-setup-vcjcz8` رو خودکار (بعد از یک build gate واقعی) رو سرور دیپلوی می‌کنه. جزئیات و راه‌اندازی اولیه در `docs/ci-cd-setup.md`. **این محیط Claude Code همچنان خودش SSH مستقیم نداره** (فقط پراکسی HTTP/HTTPS به میزبان‌های مجاز) — یعنی برای هر عملیات دستی خارج از CI/CD (مثل چک‌کردن لاگ زنده‌ی سرور)، دستورها رو باید *به کاربر* داد تا خودش رو سرور اجرا کنه.
9. Dockerfile بک‌اند فقط `src/` و `package.json` رو کپی می‌کرد — اگه فایل جدیدی مثل `scripts/` اضافه کردی، حتماً `COPY` مربوطه رو هم به Dockerfile اضافه کن، وگرنه توی کانتینر نیست حتی اگه توی ریپو باشه.
10. Next.js standalone output (در `agentos-web` Docker image) پیش‌فرض روی متغیر `HOSTNAME` بایند می‌شه که Docker خودکار به container ID ست می‌کنه — نه `0.0.0.0`. این باعث fail شدن HEALTHCHECK و هر تست `localhost` داخل کانتینر می‌شه. Dockerfile الان صریح `ENV HOSTNAME=0.0.0.0` داره؛ اگه یه‌بار دیگه container یهو unhealthy شد، همینو چک کن.
11. تماس‌های AI Gateway (`callAnthropic`/`callOpenAI` در `agent.js`) یک timeout ۲۵ ثانیه‌ای دارن (`AI_GATEWAY_TIMEOUT_MS`). قبلاً نداشتن و اگه GapGPT کند/بی‌پاسخ می‌شد، درخواست برای همیشه "در حال پردازش" می‌موند بدون هیچ خطایی تو لاگ.
12. تاریخچه‌ی چت (`ChatPanel.tsx`) تو `localStorage` مرورگر ذخیره می‌شه، با کلید per-user (`agentos_chat_messages:<userId>`) — قبلاً یک کلید ثابت بود که باعث می‌شد چت یک حساب تو حساب بعدی (روی همون مرورگر) دیده بشه. اگه فیچر مشابهی اضافه کردی، همیشه کلید `localStorage` رو namespace کن.
13. `docker compose up -d --build` کانتینر `caddy` رو ری‌استارت **نمی‌کنه** فقط چون محتوای `Caddyfile` (که volume-mount شده) عوض شده — Compose فقط وقتی سرویسی رو recreate می‌کنه که تعریف خودِ سرویس (image/env/...) عوض بشه. ✅ **دیگه دستی نیست**: `deploy.yml` حالا خودش قبل/بعد از rsync کردن `Caddyfile` چک‌سام می‌گیره و اگه عوض شده بود خودش `docker compose restart caddy` می‌زنه.
   ⚠️ **گیر واقعی که همین امروز (۲۰۲۶-۰۷-۲۵) کشف شد**: `agentos-deploy/docker-compose.yml` و `agentos-deploy/Caddyfile` تو ریپو، تا همین امروز اصلاً توسط `deploy.yml` به سرور rsync نمی‌شدن (فقط `agentos-backend/` و `agentos-web/` sync می‌شدن) — یعنی هر تغییری که من رو این دو فایل تو ریپو می‌زدم، بی‌صدا هیچ‌وقت به سرور نمی‌رسید (دقیقاً همین باعث شد اضافه‌کردن `TELEGRAM_BOT_TOKEN` به docker-compose.yml کار نکنه تا کاربر دستی رو خودِ سرور هم اضافه‌ش کرد). ✅ رفع شد: `deploy.yml` حالا این دو فایل رو هم rsync می‌کنه. **پیامد مهم**: از الان به بعد، ریپو (`agentos-deploy/`) تنها منبع حقیقته برای این دو فایل — هر ویرایش دستی مستقیم رو `~/docker-compose.yml` یا `~/Caddyfile` روی سرور، با دیپلوی بعدی **پاک می‌شه**. هر تغییری تو این دو فایل باید اول تو ریپو اعمال و push بشه.
14. تو `agentos-web`، هیچ‌جا فقط `overflow-y-auto` تنها نذار — طبق اسپک CSS، اگه overflow-x رو صریح ست نکنی، مرورگر خودش overflow-x رو هم `auto` می‌کنه (نه `hidden`)، یعنی همون کانتینر می‌تونه جدا اسکرول افقی بگیره. این با موس رو دسکتاپ اصلاً حس نمی‌شه ولی رو گوشی با لمس خیلی اذیت‌کننده‌ست. همیشه `overflow-y-auto overflow-x-hidden` با هم بنویس (همه‌ی صفحات `(app)` الان همینطورن).
15. `src/db.js` الان دو تا backend داره پشت یک API یکسان (`db.get/all/run/exec`, همه async، placeholder همیشه `?`) — هرگز مستقیم `db.prepare(...)` یا SQL مخصوص یک backend ننویس. دو گیر واقعی که فقط با Postgres واقعی (نه فقط خوندن کد) کشف شدن:
    - ستون‌های JSON (`fields_json`, `values_json`, `features_json`, `payload_json`) عمداً `TEXT` نگه داشته شدن، نه `JSONB` — چون `pg` ستون‌های JSONB رو خودکار به Object پارس می‌کنه، ولی همه‌جای کد `JSON.parse(row.xxx_json)` صدا می‌زنه (چون SQLite همیشه TEXT خام برمی‌گردونه). اگه یه ستون JSON جدید اضافه کردی، تو schema پستگرس هم `TEXT` بذار، نه `JSONB`.
    - ستون‌های `BIGINT` (تایم‌استمپ میلی‌ثانیه‌ای) از `pg` به‌صورت **string** برمی‌گردن، نه Number — `db.js` با `pg.types.setTypeParser(20, ...)` این رو به Number تبدیل می‌کنه (چون فرانت جاهایی مثل `new Date(row.created_at)` عدد می‌خواد، و روی string غیر-ISO نتیجه `Invalid Date` می‌ده). اگه یه ستون BIGINT جدید اضافه کردی، همین رفتار خودکار شامل حالش می‌شه؛ فقط یادت باشه چرا اون خط تو db.js هست.
    - همچنین: مقایسه‌ی لفظی ستون‌های Boolean-مانند (`enabled`, `is_active`, `is_super_admin`) تو SQL همیشه با `= TRUE`/`= FALSE` بنویس، نه `= 1`/`= 0` — تو Postgres این ستون‌ها واقعاً `BOOLEAN`ن و `col = 1` خطای type می‌ده (ولی پارامتر bind‌شده با مقدار JS عدد ۰/۱ مشکلی نداره، چون Postgres رشته‌ی `'0'`/`'1'` رو boolean معتبر می‌دونه — فقط literal تو خودِ متن SQL مشکل‌سازه).
16. **مکالمه صوتی با Agent** (۲۰۲۶-۰۷-۲۶) — هم تو وب‌چت (`ChatPanel.tsx`، دکمه 🎙️) هم تو تلگرام (وویس بفرست، وویس جواب بگیر)، از طریق `agentos-backend/src/voice.js`:
    - **یک کلید کاملاً جدا از AI متنی**: `OPENAI_VOICE_API_KEY`/`OPENAI_VOICE_BASE_URL` — عمداً از `OPENAI_API_KEY`/`OPENAI_BASE_URL` (که تو این پروژه معمولاً GapGPT‌ه) جداست، چون کیفیت صدا (به‌خصوص فارسی) وابسته به اینه که واقعاً به endpoint های خودِ OpenAI (`/audio/transcriptions`, `/audio/speech`) برسیم، نه یک reseller که فقط متن رو proxy می‌کنه. کاربر گفت یک کلید واقعی و مستقیم OpenAI برای این جدا می‌گیره. مدل‌های پیش‌فرض: STT=`gpt-4o-transcribe`، TTS=`gpt-4o-mini-tts` (نسخه‌های جدیدتر، کیفیت فارسی بهتر از `whisper-1`/`tts-1` قدیمی — دقیقاً همونی که کاربر خواسته بود). بدون این کلید، `voice.voiceEnabled()` false برمی‌گرده و کل فیچر بی‌صدا غیرفعاله (نه کرش) — هم وب هم تلگرام پیام «قابلیت صوتی فعال نشده» می‌دن.
    - **وب**: `POST /api/voice/transcribe` (بدنه JSON `{audioBase64, mimeType}` — نه multipart، چون از `readBody` معمولی بزرگ‌تره؛ یک `readBodyLimited` جدا با سقف ۸ مگابایت اضافه شد، سقف ۱ مگابایتی `readBody` معمولی دست‌نخورده موند) و `POST /api/voice/speak` (بدنه JSON `{text}`، پاسخ raw bytes با `Content-Type: audio/mpeg`، نه JSON). فرانت (`ChatPanel.tsx`) از `MediaRecorder` واقعی مرورگر (نه `SpeechRecognition` قدیمی) برای ضبط استفاده می‌کنه، Blob رو base64 می‌کنه و می‌فرسته؛ برای پخش پاسخ هم فایل mp3 برگشتی رو با `<audio>`/`Audio()` پخش می‌کنه. پروکسی Next.js (`/api/proxy/[...path]/route.ts`) از قبل بدون هیچ تغییری این رو ساپورت می‌کرد — چون از قبل برای دانلود گزارش‌های `.xlsx`/`.csv` مسیر passthrough باینری (غیر-JSON) داشت.
    - **تلگرام** (`telegram.js`): وویس ورودی (`msg.voice`) با `getFile` + دانلود از `/file/bot<token>/<path>` گرفته می‌شه (همون host فیلترشده‌ی api.telegram.org، پس از همون `TELEGRAM_SOCKS_PROXY` هم رد می‌شه)، `transcribeAudio()` می‌شه، از همون `act()` پایپ‌لاین مشترک با متن رد می‌شه، و جواب با `sendVoice` (آپلود multipart واقعی، فرمت الزامی تلگرام برای voice bubble گرد: **OGG container با کدک Opus** — به همین خاطر `synthesizeSpeech(text, 'opus')` صدا زده می‌شه، نه mp3) برمی‌گرده. اگه اکشن حساس بود (نیاز به تایید)، جواب همیشه متنه با دکمه‌های شیشه‌ای (نه صدا) — منطقی نیست تایید/رد رو با صدا نشون بدی.
    - چون `undici`'s fetch با `socks-proxy-agent` سازگار نیست (گیر قدیمی همین فایل)، آپلود multipart وقتی پروکسی فعاله از `node:https` دستی رد می‌شه (`callTelegramFile`/`buildMultipartBody`/`requestRaw`، نه از FormData مرورگری) — الگوی مشابه `requestViaAgent` قبلی، فقط برای بدنه‌ی باینری/multipart به‌جای JSON.
    - ⚠️ **هنوز روی سرور واقعی تست نشده** (این sandbox نه به api.openai.com نه به api.telegram.org دسترسی داره) — تست‌ها (`test/voice.test.js`, `test/telegram-voice.test.js`) یک سرور HTTP محلی جعلی می‌سازن که دقیقاً همون JSON/multipart contract واقعی OpenAI Audio API و Bot API رو پیاده می‌کنن؛ منطق کامل (فراخوانی صحیح مدل/voice، هندل خطا، مسیر Approval Gate با/بدون صدا، آپلود multipart) واقعاً تست شده، فقط transport و کیفیت واقعی صدا جعلیه. قبل از اعتماد کامل، باید یک بار دستی رو سرور واقعی (بعد از ست‌کردن `OPENAI_VOICE_API_KEY` تو `.env`) چک بشه — هم دکمه میکروفون تو وب، هم فرستادن وویس به بات تلگرام.

## پلن‌ها

(تومان، منبع حقیقت: جدول `plans` در دیتابیس، نه کد هاردکد)
Free (۰) → Starter (۹۹۰هزار/ماه) → Pro (۲.۹۹۹میلیون/ماه، محبوب‌ترین) → Enterprise (شروع از ۹.۹میلیون/ماه، سقف سالانه ۹۹میلیون)

## فازهای باقی‌مانده (اولویت‌بندی‌شده)

1. ✅ ~~رفع بدهی فنی create_contact/create_deal~~ — فاز ۱ کامل شد و روی production تأیید شد. جزئیات کامل (schema، تصمیم‌ها، نتیجه دیپلوی) در `docs/phase1-event-schema-agent-roles.md`.
2. ✅ ~~HTTPS واقعی~~ — DNS جا افتاد، Caddy گواهی Let's Encrypt گرفت، `ALLOW_INSECURE_COOKIE` حذف شد
3. ✅ ~~راه‌اندازی CI/CD ساده~~ — `.github/workflows/deploy.yml` (build gate + دیپلوی خودکار SSH). راهنمای setup در `docs/ci-cd-setup.md`
4. ✅ ~~Marketplace UI در Next.js~~ — صفحه `/marketplace` اضافه شد (انتشار ماژول خودت + نصب از کاتالوگ مشترک)، بک‌اندش از قبل آماده بود
5. ✅ ~~مهاجرت PostgreSQL~~ — `src/db.js` الان از هر دو backend پشتیبانی می‌کنه (SQLite پیش‌فرض، Postgres وقتی `DATABASE_URL` ست بشه)، با یک PostgreSQL 16 واقعی تست شد (نه شبیه‌سازی) — کل تست‌suite بک‌اند + یک اجرای دستی end-to-end. جزئیات، دو باگ واقعی که فقط با تست روی Postgres واقعی پیدا شدن (JSONB auto-parse، BIGINT-as-string)، و راهنمای فعال‌سازی در `agentos-deploy/DEPLOY.md`. **هنوز واقعاً روی SQLite در production هستیم** — این کار فقط زیرساخت رو آماده کرد؛ فعال‌سازی واقعی Postgres روی سرور یک تصمیم جداست (وقتی واقعاً به چند Instance نیاز شد). RLS (`schema-postgres.sql`) هم مستنده ولی عمداً هنوز وایر نشده — به همون فایل مراجعه کن.
6. Zarinpal واقعی — **عمداً به تعویق افتاد**: دامنه فعلی (`exirsms.ir`) فقط تستیه، برای پروژه نهایی نیست؛ گرفتن Merchant ID روی این دامنه بعداً موقع مهاجرت به دامنه اصلی دردسر می‌سازه. وقتی دامنه نهایی مشخص شد، اول اون رو ست کن، بعد Zarinpal.
7. ✅ ~~تأخیر ۱۲ساعته‌ی اعمال تغییر نقش/دسترسی~~ — بدون نیاز به زیرساخت کامل Refresh Token: `requireAuth()`/`requireSuperAdmin()` تو `server.js` الان `role` و `is_super_admin` رو هم (مثل `status`) هر بار مستقیم از DB می‌خونن، نه از claim توکن. یعنی تغییر نقش یا ارتقا/تنزل super-admin از همون درخواست بعدی روی توکن قبلاً صادرشده اعمال می‌شه، بدون re-login. جزئیات در «کارهای اضافه‌ای» پایین. (Refresh Token/Revocation List واقعی هنوز نیست — اگه یه‌جا نیاز به logout فوری همه‌ی نشست‌های یک کاربر یا invalidate کردن یک توکن مشخص قبل از expire شدنش پیش اومد، هنوز باید اضافه بشه؛ ولی گپ اصلی که تو این پروژه شناسایی شده بود بسته شد.)
8. **درخواست بزرگ کاربر (۲۰۲۶-۰۷-۲۵) — فقط فاز ۱ (باگ‌های فوری + اتوماسیون ماژول) انجام شد، ۷ آیتم دیگه هنوز باقی‌مونده:**
   - ✅ ~~تب‌های داشبورد سوپرادمین رو موبایل نمایش داده نمی‌شدن~~ — تب‌بار `admin/page.tsx` از `overflow-x-auto` (اسکرول افقی بدون هیچ نشونه‌ی بصری) به `flex-wrap` تغییر کرد؛ الان هر ۵ تب همیشه دیده می‌شن (تست شده با Playwright روی viewport 390px).
   - ✅ ~~نصب تکراری ماژول از Marketplace~~ — یک باگ واقعی بود: `module.installed` هیچ چکی برای نصب تکراری نداشت. الان `custom_modules.source_market_id` ستون جدید داره، و `dispatch()` قبل از INSERT چک می‌کنه؛ نصب دوم رکورد جدید نمی‌سازه، همون رکورد قبلی رو برمی‌گردونه (`type: 'already_installed'`, HTTP 409). تست regression: `test/actions.test.js`.
   - ✅ ~~عکس پروفایل در چت~~ — بدون هیچ زیرساخت آپلود/ذخیره‌سازی: آواتار دایره‌ای رنگی deterministic (هش‌شده از userId/agentName) با حرف اول اسم برای کاربر، و ایموجی 🤖 برای Agent، تو `ChatPanel.tsx`. اگه بعداً عکس واقعی (آپلود یا از Gmail OAuth) خواستی، این فقط باید جایگزین بشه، نه اضافه — همون `Avatar` component رو نگه دار.
   - ✅ ~~ماژول‌ها فقط فرم بودن، هیچ اتوماسیونی نداشتن~~ — یک موتور واقعی اضافه شد: جدول جدید `module_automations` (trigger فعلاً فقط `record_created`؛ ستون `trigger` برای گسترش آینده مثل `record_updated` از قبل هست). دو نوع اکشن پیاده‌سازی شده: `create_task` (با template interpolation، مثلاً `{{name}}`) و `webhook` (POST واقعی، best-effort با timeout ۸ ثانیه، هیچ‌وقت باعث fail شدن ثبت رکورد نمی‌شه). منطق اجرا در `actions.js` (`runAutomations`, داخل `module.record_created` apply())، API در `server.js` (`GET/POST /api/modules/:id/automations`, `PATCH`/`DELETE .../:automationId`)، UI در `modules/page.tsx`. تست کامل end-to-end (هم `node:test` هم دستی با Playwright — واقعاً یک رکورد ساختم و دیدم task خودکار با متن interpolate-شده تو صفحه وظایف ظاهر شد).
   - ⏳ **باقی‌مونده (کاربر صراحتاً برای فاز بعد گذاشت، تصمیم‌ها از قبل گرفته شده):**
     - تاریخ شمسی (Jalali) در کل UI — هنوز شروع نشده.
     - ✅ ~~یادآوری خودکار وظایف~~ + ~~اتصال Agent به تلگرام~~ — هر دو با هم پیاده‌سازی و تست شدن (۲۰۲۶-۰۷-۲۵، بعد از این‌که کاربر توکن بات BotFather رو مستقیم فرستاد). جزئیات کامل:
       - `agentos-backend/src/telegram.js` — بات رسمی BotFather (نه اکانت شخصی)، با **long polling** (`getUpdates`)، نه webhook — نیازی به URL عمومی/HTTPS نداره و رفتارش لوکال و روی سرور واقعی یکسانه. توکن از `TELEGRAM_BOT_TOKEN` خونده می‌شه؛ خالی بذاری یعنی کاملاً غیرفعاله (فقط یک لاگ می‌ده، هیچ کرشی نمی‌ده).
       - اتصال حساب: کاربر از «تنظیمات» یک کد ۶رقمی یک‌بارمصرف می‌گیره (`POST /api/me/telegram/link-code`, جدول `telegram_link_codes`, انقضا ۱۰ دقیقه)، تو تلگرام به بات `/start <کد>` می‌فرسته، `chat_id` به `users.telegram_chat_id` وصل می‌شه (`UNIQUE`، می‌تونه NULL باشه برای کاربرهای وصل‌نشده). قطع اتصال: `DELETE /api/me/telegram`.
       - پیام‌های تلگرامِ یک کاربر متصل، دقیقاً از همون `agent.js`'s `act()` رد می‌شن که ChatPanel وب استفاده می‌کنه — یعنی همون Approval Gate برای اکشن‌های حساس (delete_deal, issue_invoice, ...) اینجا هم هست؛ به‌جای دکمه‌های وب، دو دکمه inline keyboard («✅ تایید»/«✖️ رد») می‌فرسته که به `resolvePending()` وصلن.
       - Offset پردازش‌شده در `telegram_poll_state` (تک‌ردیفی) ذخیره می‌شه تا ری‌استارت بک‌اند باعث اجرای دوباره‌ی اکشن‌های قبلاً پردازش‌شده نشه.
       - یادآوری وظایف: `agentos-backend/src/reminders.js` — هر ۶۰ ثانیه `tasks` رو با `due_at <= now() AND reminder_sent_at IS NULL` چک می‌کنه، برای assignee با `telegram_chat_id` ست‌شده پیام می‌فرسته، و `reminder_sent_at` رو ست می‌کنه (دقیقاً یک‌بار، نه هر بار due).
       - گیر واقعی که فقط با تست پیدا شد: اگه `getUpdates` جواب non-ok برگردونه (نه فقط exception شبکه)، حلقه‌ی polling بدون هیچ delay ای پشت‌سرهم retry می‌کرد (busy-loop). فیکس: `getUpdates` حالا رو non-ok صریحاً throw می‌کنه تا همون backoff ۳ثانیه‌ای `startPolling()` براش اجرا بشه.
       - تست: چون این محیط dev sandbox دسترسی شبکه به `api.telegram.org` نداره (سیاست امنیتی خودِ محیط، نه مشکل توکن)، تست‌ها (`test/telegram.test.js`, `test/reminders.test.js`) یک سرور HTTP محلی جعلی می‌سازن که دقیقاً همون JSON contract واقعی Bot API رو پیاده می‌کنه (`TELEGRAM_API_BASE` override می‌شه) — منطق کامل (لینک‌کردن، اجرای اکشن، Approval Gate، پیشرفت offset) واقعاً تست شده، فقط transport جعلیه. روی سرور واقعی (`94.182.93.52`) که دسترسی اینترنت کامل داره، همین کد باید مستقیم کار کنه — قبل از اعتماد کامل، یک بار دستی رو سرور واقعی چک کن.
       - ⚠️ توکن بات (که کاربر مستقیم تو چت پیست کرد) توی هیچ فایل commit‌شده‌ای ذخیره نشده — فقط باید تو `.env` واقعی سرور (نه git) گذاشته بشه. چون یه بار plaintext تو یک پیام چت رفت، اگه کاربر می‌خواد کاملاً مطمئن باشه، می‌تونه از BotFather با `/revoke` یه توکن جدید بگیره.
       - ✅ **گیر واقعی روی production کشف و رفع شد (۲۰۲۶-۰۷-۲۵)**: بعد از دیپلوی، بات هیچ‌وقت جواب نمی‌داد — لاگ `backend` نشون می‌داد `[telegram] poll error: fetch failed`. تشخیص: `api.telegram.org` از داخل سرور (که ظاهراً تو ایرانه) DNS-hijack شده به یک IP داخلی/خصوصی (`10.10.34.36`) — حتی با query مستقیم از `8.8.8.8` هم همون IP جعلی برمی‌گرده، یعنی خودِ شبکه (نه فقط resolver محلی) جواب DNS رو دستکاری می‌کنه؛ دقیقاً همون فیلترینگی که GapGPT رو برای OpenAI لازم کرد. راه‌حل: `agentos-backend/src/telegram.js` الان از `TELEGRAM_SOCKS_PROXY` (اگه ست بشه) پشتیبانی می‌کنه — یک SOCKS5 proxy (مثل v2ray/shadowsocks/ssh tunnel به یه سرور خارج از فیلترینگ) که تمام تماس‌های Telegram API ازش رد می‌شن، به‌جای اتصال مستقیم. چون undici's fetch dispatcher با `socks-proxy-agent` (که Node's کلاسیک `http.Agent` interface رو پیاده می‌کنه) سازگار نیست، این مسیر از `node:https` + `SocksProxyAgent` مستقیم استفاده می‌کنه (نه از `fetch`)؛ وقتی `TELEGRAM_SOCKS_PROXY` ست نباشه، رفتار دقیقاً قبلیه (همون `fetch`، تست‌ها هم از همین مسیر رد می‌شن). تست واقعی (نه mock): `test/telegram-proxy.test.js` یک SOCKS5 server مینیمال واقعی (فقط با `node:net`, بدون dependency) + یک HTTPS server واقعی با گواهی self-signed (`openssl`) می‌سازه و ثابت می‌کنه یک درخواست HTTPS واقعاً از تونل SOCKS5 رد می‌شه (نه مستقیم). **هنوز باید کاربر خودش `TELEGRAM_SOCKS_PROXY=socks5://...` رو با پروکسی واقعی خودش (v2ray/shadowsocks که از قبل داره) تو `.env` سرور ست کنه** — من نمی‌تونم پروکسی واقعی provision کنم یا از این محیط dev بهش وصل بشم (این sandbox هم اصلاً به `api.telegram.org` دسترسی نداره، برای دلیل کاملاً متفاوت: سیاست شبکه خودِ محیط، نه فیلترینگ ایران).
       - ⚠️ **نکته مهم (همون روز کشف شد)**: پروکسی واقعی کاربر یک لینک `vmess://` (V2Ray/Xray) بود، نه یک SOCKS5 خام — `SocksProxyAgent` مستقیماً `vmess://` رو نمی‌فهمه. برای این حالت، یک سرویس جدید `xray` به `agentos-deploy/docker-compose.yml` اضافه شد که `TELEGRAM_VMESS_LINK` (خودِ لینک vmess، تو `.env` سرور، هرگز commit نمی‌شه) رو می‌خونه، `agentos-deploy/xray/entrypoint.sh` (بدون هیچ secret داخلش — فقط منطق decode عمومی، با `sh`/`grep`/`sed`/`base64`، بدون نیاز به jq/python3) اون رو به یک Xray config واقعی تبدیل می‌کنه، و یک SOCKS5 محلی بدون auth رو `xray:1080` (فقط داخل شبکه Docker) بالا میاره. `backend`'s `TELEGRAM_SOCKS_PROXY` باید به `socks5://xray:1080` اشاره کنه.
         - ⚠️ **گیر واقعی دیگه**: اولین تلاش از `image: teddysun/xray:latest` (Docker Hub) استفاده می‌کرد، ولی روی سرور واقعی با خطای `403 Forbidden` از Docker Hub رد شد (در حالی که `node:22-alpine` و `alpine` — ایمیج‌های رسمی `library/*` — قبلاً همون سرور بدون مشکل pull شده بودن؛ یعنی این ۴۰۳ مخصوص همون ریپوی خاص بود، نه یک بلاک کلی روی Docker Hub). راه‌حل: `agentos-deploy/xray/Dockerfile` خودش Xray-core رو مستقیم از GitHub release دانلود و بیلد می‌کنه (به‌جای وابستگی به یک ایمیج شخص ثالث)، پس `xray:` سرویس تو compose الان `build: ./xray` هست نه `image:`.
         - تست شده تو dev sandbox (دانلود واقعی release، اجرای واقعی باینری Xray با config واقعی تولیدشده از لینک کاربر — نه شبیه‌سازی): باینری واقعاً اجرا شد، SOCKS5 رو `1080` واقعاً بالا اومد، و `curl -x socks5h://127.0.0.1:1080` واقعاً یک SOCKS5 CONNECT رو با موفقیت (`SOCKS5 request granted`) رد کرد. تنها چیزی که از این sandbox قابل تأیید نبود، رسیدن واقعی داده تا مقصد نهایی (`engvm.dadashjan.space:2027`) بود — چون این محیط dev هم دسترسی TCP خام نداره (فقط HTTPS از پروکسی مجاز، برای دلیلی کاملاً جدا از فیلترینگ ایران). باید روی سرور واقعی با `docker compose logs xray` و یک پیام واقعی تلگرام تأیید نهایی بشه.
       - ⚠️ **گیر واقعی سوم (بعد از دیپلوی xray، هنوز رفع نشده بود)**: بعد از بالا اومدن `xray` و ست‌شدن `TELEGRAM_SOCKS_PROXY=socks5://xray:1080`، بک‌اند این‌بار خطای جدید می‌داد: `Client network socket disconnected before secure TLS connection was established` (نه دیگه `fetch failed`). علتش: پکیج `socks-proxy-agent` وقتی پروتکل URL دقیقاً `socks5://` باشه، هاست‌نیم (`api.telegram.org`) رو **خودش، محلی، قبل از رسیدن به پروکسی** resolve می‌کنه (دقیقاً همون DNS مسموم سرور) — فقط `socks5h://` (یا `socks://` بدون نسخه) resolve رو به خودِ پروکسی می‌سپاره. یعنی تا الان تونل SOCKS5 درست وصل می‌شد ولی به یک IP اشتباه (همون IP جعلی DNS مسموم) که چیزی پشتش نبود. راه‌حل: `getProxyAgent()` تو `telegram.js` حالا هر `socks5://`/`socks4://` رو خودکار به `socks5h://`/`socks4a://` تبدیل می‌کنه، صرف‌نظر از این‌که تو `.env` چی نوشته شده — یعنی کاربر مجبور نیست حواسش به این جزئیات پروتکل باشه. تست واقعی (نه mock): `test/telegram-proxy.test.js` یک دامنه‌ی کاملاً ساختگی (`fake-telegram.test`, هیچ‌جا تو DNS واقعی وجود نداره) رو از طریق پروکسی درخواست می‌کنه و چک می‌کنه سرور SOCKS5 خودِ این هاست‌نیم رو (نه یک IP از قبل resolve‌شده) دریافت کرده — اگه resolve محلی اتفاق می‌افتاد، Node قبل از رسیدن به SOCKS اصلاً ENOTFOUND می‌داد.
     - کنترل کامل tenant در پنل سوپرادمین (تعداد کاربر، محدودیت ماژول، و غیره) — بخشی از این از قبل هست (`PATCH /api/admin/tenants/:id` planKey/status رو عوض می‌کنه، و `plans.seats_limit`/`modules_limit` از قبل اعمال می‌شن)؛ چیزی که نیست: override دستی per-tenant مستقل از پلن (مثلاً «این یکی tenant حتی تو پلن Free بشه ۱۰ کاربر داشته باشه»).
     - لاگین با Gmail یا OTP — کاربر به سوال «OAuth Client ID/Secret از قبل تو Google Cloud Console ساختی؟» با «چیز دیگه‌ای» جواب داد، یعنی جزئیاتش هنوز روشن نیست؛ قبل از شروع باید دقیق پرسیده بشه چی داره/نداره، و برای OTP باید یک SMS gateway انتخاب بشه (تصمیم گرفته نشده).

## کارهای اضافه‌ای که خارج از این لیست انجام شد (ولی مهم بودن)

- ✅ تست‌های خودکار بک‌اند (`agentos-backend/test/`, با `node:test` بدون dependency جدید) برای `dispatch`/Approval Gate/ایزوله‌بودن Tenant — به CI (`npm test` در `build-check`) وصل شدن، چون این‌ها دقیقاً چیزهاییه که فاز ۱ رو migrate کردیم و فقط با curl دستی تست شده بودن.
- ✅ Super Admin Dashboard در Next.js (`/admin`) — قبلاً فقط تو `frontend-local/admin.html` (HTML خام) بود. همه‌ی ۵ تب (KPI، Tenantها، پلن‌ها، Marketplace، Audit Log) رو داره؛ بک‌اندش تغییری نکرد.
- ✅ کل UI موبایل/ریسپانسیو شد: منوی sidebar روی موبایل به drawer قابل‌باز/بسته تبدیل شد (`components/AppShell.tsx`)، همه‌ی جدول‌ها به‌جای شکستن layout خودشون جدا اسکرول افقی می‌گیرن، و باگ واقعی `overflow-y-auto` (گیر #۱۴ بالا) که فقط رو گوشی با لمس قابل کشف بود پیدا و فیکس شد.
- ✅ یک بازبینی امنیتی کامل انجام شد (auth، ایزوله‌بودن Tenant، کدهای جدید فاز ۱/Admin/Marketplace). ۴ مورد واقعی پیدا و فیکس شد:
  - XSS ذخیره‌شده تو تولید PDF گزارش (`reports/page.tsx`) — اسم مخاطب/معامله بدون escape تو HTML تزریق می‌شد؛ با DOM API امن (`textContent`) بازنویسی شد.
  - حذف/غیرفعال‌کردن پرسنل واقعاً دسترسی رو لغو نمی‌کرد — توکن قدیمی تا ۱۲ ساعت کار می‌کرد و حتی می‌شد دوباره لاگین کرد؛ الان `requireAuth()` و `/api/auth/login` وضعیت زنده‌ی `users.status` رو چک می‌کنن.
  - تزریق فرمول تو خروجی CSV/Excel گزارش‌ها (مثل اسم مخاطب `=cmd|...`) — با prefix کردن `'` (استاندارد OWASP) خنثی شد، هم تو CSV هم تو `.xlsx` واقعی.
  - یک رگرسیون واقعی از فاز ۱: `billing.js` هنوز تو جدول حذف‌شده‌ی `audit_logs` می‌نوشت (تایید پرداخت واقعی زرین‌پال رو crash می‌کرد) — به `audit()` استاندارد (که رو `events` می‌نویسه) منتقل شد.
  - ✅ **رفع شد (بعد از بازبینی امنیتی اولیه)**: تغییر نقش کاربر (مثلاً admin→member یا برعکس) و ارتقا/تنزل super-admin دیگه منتظر expire شدن توکن (تا ۱۲ ساعت) نمی‌مونن. `requireAuth()` و `requireSuperAdmin()` تو `agentos-backend/src/server.js` حالا `role`/`is_super_admin` رو از ردیف زنده‌ی `users` می‌خونن (دقیقاً مثل کاری که قبلاً برای `status` انجام شده بود)، نه از payload امضاشده‌ی توکن. تست integration واقعی (نه mock) این رفتار رو قفل کرده: `agentos-backend/test/auth-staleness.test.js` — یک توکن قدیمی رو می‌گیره، نقش کاربر رو از سمت owner عوض می‌کنه، و چک می‌کنه که همون توکن قدیمی (بدون re-login) بلافاصله نقش جدید رو منعکس کنه؛ تست دوم مشابه برای حذف عضو تیم. برای اجرای این ری‌فکتور کوچیک، `server.listen()` تو `server.js` پشت `if (require.main === module)` قرار گرفت تا require کردن ماژول تو تست بدون بایند کردن یک پورت واقعی کار کنه (رفتار اجرای مستقیم `node src/server.js` تغییری نکرده).
