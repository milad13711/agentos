# Secrets — never commit real environment values, only *.env.example files
.env
.env.local
.env.*.local
*.env
!*.env.example

# Backend: local SQLite database (persistent runtime data, not source)
agentos-backend/data/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output
agentos-web/.next/
agentos-web/out/

# General build/OS noise
dist/
build/
.DS_Store
*.log
